class AccountEditor {

}

let aE = new AccountEditor();
let args = process.argv.slice(2, process.argv.length);

switch (args[0]) {
    case "edit":
        break;

    case "replace":
        break;
}